using System;

namespace Ejercicio2
{
    class Program
    {
        static void Main()
        {
            Console.WriteLine("Introduce la cantidad de numeros");
            int cantidad = Convert.ToInt32(Console.ReadLine());
            Random numeros = new Random();
            Console.WriteLine("Elige el primer numero");
            int numero1 = Convert.ToInt32(Console.ReadLine());
            Console.WriteLine("Elige el ultimo numero");
            int numero2 = Convert.ToInt32(Console.ReadLine());


            for (int i = 0; i < cantidad; i++)
            {
                Console.WriteLine("El numero aleatorio es " + numeros.Next(numero1, numero2));
            }
            Console.ReadKey();
        }
        public void numeros(int numero1, int numero2)
        { }

    }
}
